<Page
    x:Class="CubeOS95.BIOS_Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CubeOS95"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#0000ac"
    RequestedTheme="Dark">

    <Grid x:Name="RootGrid">

        <Border x:Name="MainBorder" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="#fcfcf9" BorderThickness="3,1,3,1" Margin="2,1,2,1">
            <Grid x:Name="MainMenuGrid" Height="684" Width="458" Margin="1,1,1,1" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="#fcfcf9" BorderThickness="3,1,3,1">
                <Border BorderBrush="#fcfcf9" BorderThickness="0,0,0,1" Height="80" VerticalAlignment="Top">
                    <Grid x:Name="SetupUtilityGrid" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="80">
                        <TextBlock Text="SHS Setup Utility  -  (cos) 1994" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,16" FontSize="19" FontFamily="/Assets/Fonts/DOSFont.ttf#DOSFont"/>
                    </Grid>
                </Border>
                <Border BorderBrush="#fcfcf9" BorderThickness="0,0,0,1" Height="239" VerticalAlignment="Top" Margin="0,80,0,0">
                    <Grid x:Name="PCOptionsGrid" HorizontalAlignment="Stretch" VerticalAlignment="Top">

                    </Grid>
                </Border>
                <Border BorderBrush="#fcfcf9" BorderThickness="0,0,0,1" Height="279" Margin="0,239,0,0">
                    <Grid x:Name="QuitBIOSGrid" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="279">
                        <Button x:Name="ExitWithoutSavingButton" HorizontalContentAlignment="Left" Width="295" Height="49" CornerRadius="0" IsTabStop="False" UseSystemFocusVisuals="False" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="28,85,0,0" Click="ExitWithoutSavingButton_Click">
                            <Button.Resources>
                                <ResourceDictionary>
                                    <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
                                    <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
                                    <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
                                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#c1272d"/>
                                    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="#fcf536"/>
                                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#c1272d"/>
                                    <SolidColorBrush x:Key="ButtonForegroundPressed" Color="#fcf536"/>
                                    <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#c1272d"/>
                                    <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#c1272d"/>
                                </ResourceDictionary>
                            </Button.Resources>
                            <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Orientation="Horizontal">
                                <TextBlock Text=">" FontSize="20" VerticalAlignment="Center" Margin="-12,0,0,0" HorizontalAlignment="Left" FontFamily="/Assets/Fonts/DOSFont.ttf#DOSFont"/>
                                <TextBlock Text="Exit Without Saving" FontSize="20" VerticalAlignment="Center" Margin="11,0,0,0" HorizontalAlignment="Left" FontFamily="/Assets/Fonts/DOSFont.ttf#DOSFont"/>
                            </StackPanel>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal">
                                                                <Storyboard>
                                                                    <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="PointerOver">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="Background">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBackgroundPointerOver}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="BorderBrush">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBorderBrushPointerOver}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                         Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonForegroundPointerOver}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="Background">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBackgroundPressed}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="BorderBrush">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBorderBrushPressed}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                         Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonForegroundPressed}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Disabled">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="Background">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBackgroundDisabled}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid"
                         Storyboard.TargetProperty="BorderBrush">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonBorderBrushDisabled}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                         Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ButtonForegroundDisabled}" />
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>
                                                    <ContentPresenter x:Name="ContentPresenter"
BorderBrush="Transparent"
BorderThickness="{TemplateBinding BorderThickness}"
Content="{TemplateBinding Content}"
ContentTransitions="{TemplateBinding ContentTransitions}"
ContentTemplate="{TemplateBinding ContentTemplate}"
Padding="{TemplateBinding Padding}"
HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
AutomationProperties.AccessibilityView="Raw" />
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>